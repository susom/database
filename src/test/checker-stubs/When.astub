import org.checkerframework.checker.tainting.qual.Untainted;

package com.github.susom.database;

@Untainted
class When {
  @Untainted When oracle(@Untainted String sql);

  @Untainted When derby(@Untainted String sql);

  @Untainted When postgres(@Untainted String sql);

  @Untainted String other(@Untainted String sql);

  @Untainted String toString();
}
